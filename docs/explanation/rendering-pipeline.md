# The rendering pipeline

Explanation. How a page becomes bytes on the wire, stage by stage — parts and
furniture laid into frames, the attribute planner, the fill algorithm, the
encoding. Content comes later.
