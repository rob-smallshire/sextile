# Command line

Reference: the `sextile` command and the pieces a service builds its own command
line from. For now the authority is the {py:mod}`sextile.cli` module page in the
{doc}`API reference <api/index>`; this page will document the subcommands and
their options directly.
