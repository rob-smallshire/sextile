# Keys

Reference: the keys a reader presses and what each means. For now the authority
is the {py:mod}`sextile.keys` module page in the {doc}`API reference <api/index>`;
this page will gather the same constants into a table a service author can read
at a glance.
