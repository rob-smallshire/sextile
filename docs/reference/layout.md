# Layout

Reference: the parts, furniture and page shapes a page is built from. For now the
authority is the {py:mod}`sextile.layout` and {py:mod}`sextile.formatting` module
pages in the {doc}`API reference <api/index>`; this page will lay the pieces out
as a map from what a page looks like to the type that draws it.
