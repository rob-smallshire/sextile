# Viewdata encoding

Reference: what the BBC end actually does on the wire, and how it was measured.
The write-up lives at `packages/sextile/docs/viewdata-encoding.md` for now —
already reference-shaped, and left at its one home this pass because the skeleton
does not move content out of the packages. The Phase 4 content pass brings it
here verbatim and makes that file a pointer, as the glossary already is.
