# The framework's pages, and drawing your own

A tutorial step. By the end the service offers the framework's own history,
contents and keyword pages, draws a month as a grid with a `Custom` part, and
rings off on `*90#`.
