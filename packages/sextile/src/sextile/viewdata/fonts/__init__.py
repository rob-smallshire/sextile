"""The faces shipped with the framework.

Each `.font` file here is third-party material converted into this project's
own format by a script in `tools/`, and carries its source and terms in its own
header as well as in the repository's NOTICE.md. Nothing is vendored here whose
terms have not been read.
"""
